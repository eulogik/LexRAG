# Lightweight dev/test dependencies — no torch, no embedding models.
# Full stack lives in requirements.txt / pyproject dependencies.
pytest
pytest-cov
ruff
build
httpx
fastapi>=0.136.0
sqlite-utils
python-dotenv
